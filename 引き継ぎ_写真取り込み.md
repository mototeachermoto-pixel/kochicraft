# 写真取り込みの引き継ぎ（次のセッションへ）

トークン上限で中断。ここから再開してください。

## 今すぐやること（優先順）

### ① 判別済みだが未取り込みの2フォルダ（最優先・すぐ終わる）

以下は中身を見て判別済み。まだリサイズ・配置・コード挿入をしていない。

**Statue of Sakamoto Ryoma（5枚・5か所ちょうど。spotId: `ryoma-statue`）**
| ファイル名 | 割り当てID | 中身 |
|---|---|---|
| Gemini_Generated_Image_mcc2sumcc2sumcc2.jpg | `statue` | 見上げアングルの像本体 |
| Gemini_Generated_Image_spe8qqspe8qqspe8.jpg | `deck` | 足場（展望デッキ）が組まれた「龍馬に大接近」の写真 |
| Gemini_Generated_Image_ly7nn2ly7nn2ly7n.jpg | `sea` | 像のシルエット越しに太平洋（逆光） |
| Gemini_Generated_Image_elhcrxelhcrxelhc.jpg | `monument` | 白紙の石碑（記念碑） |
| Gemini_Generated_Image_11xio911xio911xi.jpg | `sign` | 像＋白紙の案内板（看板が手前） |

**Makino Botanical Garden（6枚・判別3/6のみ完了。spotId: `makino`）**
| ファイル名 | 割り当てID | 中身 |
|---|---|---|
| Gemini_Generated_Image_39152a39152a3915.jpg | `hall` | 波打つ屋根の記念館＋池の木道 |
| Gemini_Generated_Image_7svozk7svozk7svo.jpg | `plants` | 花いっぱいの園路（Many Plants） |
| Gemini_Generated_Image_f9hmd2f9hmd2f9hm.jpg | `pond` | 池にかかる木道（睡蓮） |
| Gemini_Generated_Image_habbpkhabbpkhabb.jpg | **未確認** | ← 次はこれを Read で開いて中身確認 |
| Gemini_Generated_Image_sriq1isriq1isriq.jpg | **未確認** | 残り候補：`greenhouse`（温室）／`sueko`（スエコザサ＝低い笹の接写）／`sign`（看板） |
| Gemini_Generated_Image_w73dy3w73dy3w73d.jpg | **未確認** | 同上 |

### ② まだ中身を全く見ていない2フォルダ

**Noichi Zoo（6枚）** — spotId: `noichi`。objects は7個（`shoebill` `giraffe-zebra` `chimp` `capybara` `lemur` `redpanda` `sign`）なので**1つ足りない**（先生に確認 or 6個だけ入れて`sign`は保留）。
- ファイル: aj3kx3 / legs7l / olp9uo / uprkkz / ur1kwx / y0cyk3（まだ1枚も開いていない）

**Ryugado Cave（6枚）** — spotId: `ryugado`。objects は6個（`stalactites` `pot` `falls` `stream` `cave` `sign`）で**枚数がちょうど一致**。
- ファイル: 9nnc2v / mcr07u / sc9rcw / si04gz / ug5dky / xif5vm（まだ1枚も開いていない）

## 進め方（テンプレート・過去と同じ手順）

1. `Read` ツールで各画像を1枚ずつ開いて中身を確認し、上の表を埋める
2. 重複チェック（念のため）：
   ```bash
   cd "マインクラフト風アプリ" && md5sum "Noichi Zoo"/* "Ryugado Cave"/* | sort | uniq -w32 -D
   ```
3. リサイズ＋JPEG変換＋配置（Python/PIL、横1200px・品質82）：
   ```bash
   cd "マインクラフト風アプリ" && python3 - << 'PYEOF'
   import os
   from PIL import Image
   jobs = {
       'ryoma-statue': {'dir': 'Statue of Sakamoto Ryoma', 'map': {
           'Gemini_Generated_Image_mcc2sumcc2sumcc2.jpg': 'statue',
           'Gemini_Generated_Image_spe8qqspe8qqspe8.jpg': 'deck',
           'Gemini_Generated_Image_ly7nn2ly7nn2ly7n.jpg': 'sea',
           'Gemini_Generated_Image_elhcrxelhcrxelhc.jpg': 'monument',
           'Gemini_Generated_Image_11xio911xio911xi.jpg': 'sign',
       }},
       'makino': {'dir': 'Makino Botanical Garden', 'map': {
           'Gemini_Generated_Image_39152a39152a3915.jpg': 'hall',
           'Gemini_Generated_Image_7svozk7svozk7svo.jpg': 'plants',
           'Gemini_Generated_Image_f9hmd2f9hmd2f9hm.jpg': 'pond',
           # habbpk / sriq1i / w73dy3 は中身確認後にキーを決めて追加
       }},
       # noichi, ryugado も中身確認後に同様に追加
   }
   MAXW = 1200
   for spot, job in jobs.items():
       outdir = os.path.join('public', 'photos', spot)
       os.makedirs(outdir, exist_ok=True)
       for fname, key in job['map'].items():
           src = os.path.join(job['dir'], fname)
           img = Image.open(src).convert('RGB')
           w = min(img.width, MAXW); h = int(img.height * (w / img.width))
           img = img.resize((w, h), Image.LANCZOS)
           out = os.path.join(outdir, f'{key}.jpg')
           img.save(out, 'JPEG', quality=82)
           print(f'{spot}/{key}.jpg  ({w}x{h}, {os.path.getsize(out)//1024}KB)')
   PYEOF
   ```
4. 各 `src/spots/<file>.ts` の該当 `objects[].id` の直後・`guide: {` の手前に
   `image: './photos/<spotId>/<key>.jpg',` を1行差し込む（正規表現一括挿入でOK。過去に使ったスクリプトと同じ要領）
5. `npm run build`
6. プレビュー起動 → ブラウザで `SPOTS` を import し、全 `objects` の `image` を `new Image()` で読み込みテスト（成功/失敗をログ）
7. コンソールエラー無しを確認
8. `名所リスト.md` のチェック欄（☐→✅ `key.jpg`）と冒頭サマリー行（取り込み済み枚数・残り観光地）を更新

## 完了済み（このセッションより前・触らなくてよい）

- 桂浜7／高知城9／はりまや橋7／にこ淵4／四万十川6／室戸岬7／足摺岬9／ひろめ市場6 ＝ **55枚取り込み済み**
- 未取得は各1枚のみ：**にこ淵の看板**・**室戸岬の灯台**（急ぎでなければ後回しでよい）
- 使用ツール：`public/photos/<spotId>/*.jpg` に配置、`src/spots/<file>.ts` の `objects[].image` で参照。
  PowerShellの `写真を取り込む.ps1` もあるが、今回はPython(PIL)で直接処理した
- 参考：[画像プロンプト.md](画像プロンプト.md)（AIで新規に写真を作る場合のプロンプト集）
