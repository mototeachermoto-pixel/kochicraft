# KochiCraft 引き継ぎ（次のセッションへ）

前回セッションの終了時点。**写真81枚は全部そろっており、アプリは動く状態**。
残っている唯一の課題は「**タブレットから接続できない**」こと。

---

## 🔴 最優先：タブレットから開けない問題（未解決）

### 状況
PC上のブラウザからは `https://192.168.0.14:4173` で**正常に開ける**ことを確認済み
（トップページ200 OK／JS 117KB／写真218KB すべて配信OK）。
しかし**タブレットから開くと「ネットワーク接続が切れました」**となり、到達すらしない。

### 原因の候補（可能性の高い順）
1. **Windowsファイアウォールがブロックしている**（いちばん怪しい）
   - Node.js の受信接続が「プライベートネットワーク」で許可されていない
   - ⚠️ ファイアウォール設定の変更は AI 側では行わない方針。**先生自身に操作してもらうか、手順を案内すること**
2. **タブレットが別のネットワークにいる**
   - 5GHz帯と2.4GHz帯で別SSIDになっている／ゲストWi-Fiにつながっている／モバイル通信になっている
3. **ルーターのAP分離（プライバシーセパレーター）が有効**
   - 同じWi-Fiでも端末同士が通信できない設定。学校・公共のWi-Fiでよくある
4. PCのIPが変わった（DHCPのため再起動で変わる可能性あり）

### 次にやるべき切り分け（この順で）
1. PCのIPを再確認：`Get-NetIPAddress -AddressFamily IPv4`（前回は `192.168.0.14`）
2. **タブレットがPCと同じWi-Fi(SSID)か**を先生に確認してもらう
3. タブレットのブラウザで `https://192.168.0.14:4173` を開き、**エラーの文言を正確に聞く**
   - 「接続が切れました／到達できません」＝ネットワーク層の問題（1〜4）
   - 「証明書が…／安全ではありません」＝到達はしている。警告を許可すればOK
4. ファイアウォールの確認（先生に操作してもらう）
   - 設定 →「ネットワークとインターネット」→「Windows ファイアウォール」→「アプリがファイアウォールを通過できるようにする」→ Node.js の「プライベート」にチェック
5. それでもダメなら **公開トンネル**を検討（下記）

### 代替案：インターネット経由の公開URL
LANがどうしても通らない場合、`cloudflared` などでトンネルを張れば
`https://xxxx.trycloudflare.com` のような**証明書の警告が出ない公開URL**が発行でき、
学校など別ネットワークからもアクセスできる。

- ⚠️ **cloudflared / ngrok はどちらも未インストール**（前回確認済み）。導入が必要
- ⚠️ **URLを知っていれば誰でも見られる状態になる**ため、実行前に必ず先生の許可を取ること

### 起動コマンド（現状）
```bash
cd "C:\Users\user\Desktop\マインクラフト風アプリ" && npm run build
```
そのうえで、LAN公開（HTTPS）は launch.json の `kochicraft-lan` を preview_start する。
手動なら PowerShell で：
```
$env:HTTPS='1'; npm run preview -- --host
```
→ `https://192.168.0.14:4173` が発行される。

---

## ✅ 前回セッションで完了したこと（触らなくてよい）

### 1. 写真の取り込み → **81か所すべて完了**
最後の3枚（室戸岬の灯台／にこ淵の看板／のいち動物園のレッサーパンダ）を取り込み、
`名所リスト.md` も更新済み。未取得の写真はもう無い。

### 2. マップ移動（Map→Go）の着地位置を全面的に修正
`src/core/Engine.ts` の `goToObject`。全81か所を実データで検証済み・問題0件。
- **正面優先**（0°→±45°→±90°…の順に左右対称で探索。横や裏に回り込まない）
- **近い距離から探し、ちょうどよい角度になった時点で確定**（フィールド端まで下がり続けない）
- **水の上に立たない**（`findStand` が `isSolid` を見るよう修正。以前は水を地面と誤認していた）
- **登り返せない崖下に落ちない**（`MAX_SAFE_DROP = 2` ブロック）
- 見上げ／見下ろし角は**最大30度でクランプ**

### 3. 桂浜の地形修正
`src/spots/katsurahama.ts` の砂浜を z67 → **z70**（fieldの端）まで拡張。
看板の正面が崖になっていて落下していた問題を解消（見上げ角 54.6° → 4.8°）。

### 4. ジャンプの高さ
`src/config/constants.ts` の `JUMP_SPEED` を 9.2 → **13.4**（＝約3ブロック）。

### 5. 画面上部の名前バナーをクリック可能に
`src/core/Engine.ts` ＋ `src/ui/styles/main.css`。
`pointer-events: none` が原因でクリックできなかったのを修正。
バナーを押すと右の情報パネルがその対象に切り替わる。

### 6. 「今どれを見ているか」の視覚マーク
`src/core/WorldManager.ts`。
- **色：ビビッドレモン `#fff200`**（選択中の枠 `#ffd24a` と区別できる。水色→マゼンタと試して却下された経緯あり）
- **見た目：線だけの大枠**（半透明の塗りつぶし＝スモーク状は「わかりづらい」と却下済み。**復活させないこと**）
- ゆっくり明滅する

### 7. 「近くの対象」の判定ロジック（何度も試行錯誤した箇所）
`WorldManager.updateNear()`。**最終仕様は以下。安易に変えないこと**：
- 条件A：対象から **4ブロック以内**（`NEAR_FACE_MAX_DIST = 4`）
- 条件B：向いている方向から **±50度以内**（`NEAR_FACE_CONE`）
- A・B両方を満たす中で**いちばん近いもの**を選ぶ
- 距離は原則「当たり判定の枠の端まで」。ただし**枠の中にいる場合だけ focus までの実距離で測り直す**
  - ← これは「柳(willows)」のように当たり判定がフィールドの37%を覆う対象が、
    すぐ目の前の橋を押しのけてしまう問題への対処。**この分岐を消すと橋が柳に乗っ取られる**

⚠️ 過去に試して**失敗した**方法（繰り返さないこと）：
- レイキャスト（画面中央）だけで判定 → 手前を素通りして奥の対象を拾う
- すべて focus までの距離で判定 → **81か所中80か所でバナーが出なくなった**
- 距離上限30ブロック → 遠くの木に反応した

---

## 📌 参考データ

### 当たり判定が広すぎる対象（15個／9観光地）
柳と同じ問題を起こしうる。現在は上記ロジックで対処済みだが、不具合が出たらここを疑う。

| 観光地 | 対象 | フィールド占有率 |
|---|---|---|
| 龍河洞 | cave | 53% |
| 桂浜 | beach | 47% |
| 桂浜 | sea | 40% |
| 高知城 | cherry | 39% |
| 四万十川 | river | 39% |
| 室戸岬 | sea | 39% |
| はりまや橋 | willows | 37% |
| 龍河洞 | stalactites | 28% |
| 足摺岬 | sea | 27% |
| 室戸岬 | rocks | 26% |
| 四万十川 | hills | 23% |
| 桂浜 | pines | 22% |
| にこ淵 | rocks | 17% |
| 坂本龍馬像 | sea | 16% |
| 牧野植物園 | plants | 16% |

### 前回セッションで変更したファイル
- `src/core/Engine.ts`（着地ロジック／バナークリック／findStandの水判定）
- `src/core/WorldManager.ts`（近接判定／ハイライト枠）
- `src/core/WalkController.ts`（spawn に pitch 引数を追加）
- `src/config/constants.ts`（JUMP_SPEED）
- `src/spots/katsurahama.ts`（砂浜の拡張）
- `src/spots/muroto.ts` `nikobuchi.ts` `noichi.ts`（image追加）
- `src/ui/styles/main.css`（name-banner）
- `vite.config.ts` ＋ `package.json`（`@vitejs/plugin-basic-ssl` 追加。`HTTPS=1` のときだけHTTPS配信）
- `.claude/launch.json`（`kochicraft-lan` 設定を追加）※編集するのは
  **`C:\Users\user\Desktop\Cloude 教育\.claude\launch.json`** の方（アプリ側のは読まれない）

### 検証のしかた（毎回これで確認していた）
`npm run build` のあと preview を起動し、ブラウザのコンソールで
`SPOTS` を import して全81オブジェクトをループさせ、
着地位置・見上げ角・水判定・近接判定をまとめてシミュレーションする。
（1か所ずつ手で歩いて確かめるより速く、見落としがない）

---

## 💡 検討中だったこと（急ぎではない）
- 見上げ角が30度を超える対象が2つ残っている：高知城の天守(38.6°)、足摺岬の白山洞門(-56°)。
  地形側に原因がないか未調査。実害があるかは要確認
- ジャンプが3ブロックになったことで、屋根や塀の上など**意図しない場所に登れる**可能性。
  実際に遊んで気になる場所が出たら対処
