# KochiCraft 引き継ぎ（2026-09-23）

## 今の状態

- **完成・公開済み**。写真81枚・地形・音声・見切れ対応まで全部終わっている
- 公開URL（PCの電源が入っていなくても動く）：
  **https://blue-tree-7826.moto-teacher-moto.workers.dev**
- 学校のネットワークで開けるかは**まだ未確認**（次にやること）

## 公開のしくみ（今のやり方）

Cloudflare Workers に、書き出したファイル（`dist`）を手でドラッグして置いている。
更新は `公開用フォルダを更新.bat` をダブルクリック→出てきたフォルダを
Cloudflareの「新規展開」にドラッグ、の2手順。
詳しくは `C:\Users\user\Desktop\KochiCraft-公開手順.md` を見る。

## 今回やったこと：ローカルにGitリポジトリを作った

「GitHubでサーバーをつなぎたい」という要望に向けて、下ごしらえをした。

- `マインクラフト風アプリ` フォルダを `git init` した
- 写真の下書きフォルダ（Cape Ashizuri など12個、270MB）を `.gitignore` に追加
  → アプリが実際に使う写真は `public/photos`（21MB）の方で、これは含めている
- 初回コミット済み（92551a7）。合計 **20.9MB**、GitHubの制限内

**注意：これはまだ「ローカルの下ごしらえ」だけ。GitHub上にリポジトリは無い。**
先生のGitHubアカウントでの操作が必要なため、次回そこから続ける。

## 次回やること：GitHub連携

目的は「コードを直したら自動で公開版が更新される」状態にすること
（今の「バッチ実行→手でドラッグ」を卒業する）。

### 手順（次回、先生と一緒に進める）

1. **GitHubアカウントを作る**（無ければ）。moto.teacher.moto@gmail.com で登録
2. GitHubで新しいリポジトリを作る（例：`kochicraft`）
3. ローカルの `マインクラフト風アプリ` フォルダから、そのリポジトリへ push する
   ```bash
   cd "C:\Users\user\Desktop\マインクラフト風アプリ"
   git remote add origin https://github.com/【ユーザー名】/kochicraft.git
   git branch -M main
   git push -u origin main
   ```
   （ここでGitHubのログインを求められるので、先生自身の操作で）
4. Cloudflareのダッシュボードで、今の「ブルーツリー7826」に
   **GitHubリポジトリを接続**する（Gitへの接続、のような項目）
   - これで「main に push すると自動でビルド→公開」になる
   - ビルドコマンド：`npm run build`　出力フォルダ：`dist`
5. 接続できたら、以後は
   `公開用フォルダを更新.bat` も「新規展開」のドラッグも不要になる。
   コードを直して `git push` するだけで公開版が更新される

### 注意点

- GitHubへのログイン・パスワード入力は先生自身が行う（Claudeは代行しない）
- リポジトリは**公開（Public）でも非公開（Private）でもどちらでもよい**。
  非公開にしてもCloudflareからは問題なく読める
- 写真はアプリの素材として作った生成画像なので、公開リポジトリでも著作権上の問題は無い見込み
- pushしたあと、Cloudflare側の「自動デプロイ」が今の手動デプロイと衝突しないか、
  最初の1回は動作確認をすること

## 保留中の宿題（前回から持ち越し）

- **学校のネットワークで `workers.dev` が開けるか確認**（最優先）
- 子どものタブレットで英語音声が入っているか（Listen in Englishで確認）
- 高知城天守(38.6°)・足摺岬白山洞門(-56°)の見上げ角、実害があれば地形調整
- ジャンプが3ブロックになったことで意図しない場所に登れる可能性（実害が出たら対処）
